<program>
  <var name=n type=int>
    {input("val of n")} // does not add newline for some reason. Also being called twice currently xd
  </var>
    <if condition={n < 0}>
      {print("negative")}
    </if>
    <elif condition={n == 0}>
      {print("not negative nor positive")}
    </elif>
    <else>
      {print("positive")}
    </else>
</program>