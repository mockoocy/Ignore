<program>
  <var name=n type=int>
    {input("val of n")} // does not add newline for some reason.
  </var>
    <if condition={n % 2 == 0}>
      {print("even")}
    </if>
    <else>
      {print("odd")}
    </else>
</program>