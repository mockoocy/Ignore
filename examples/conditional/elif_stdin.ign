<program>
  <var name=n type=int>
    {input("val of n")}
  </var>
    <if condition={n < 0} >
      {print("negative")}
    </if>
    <elif condition={n == 0}>
      {print("not negative nor positive")}
    </elif>
    <else>
      {print("positive")}
    </else>

</program>